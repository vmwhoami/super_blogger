<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <a class="navbar-brand" href=<%= root_path %> >RUBY BLOG</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarCollapse">
    <ul class="navbar-nav mr-auto d-flex">
      <li class="nav-item active">
        <a class="nav-link" href= <%= root_path %> >Home <span class="sr-only">(current)</span></a>
      </li>
       <% if logged_in? %>
                <li class="nav-item">
                  <a class="nav-link" href=<%= new_article_path %> >New article</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href=<%= articles_path %> >Articles</a>
                </li>
                </ul>

          <%= link_to 'Logout', logout_path, method: :delete, class: "text-white mr-5" %>
          <%= link_to 'Edit your profile', edit_user_path(current_user), class: "text-white mr-5" %>
          <%= link_to 'View your profile', user_path(current_user), class: "text-white mr-5" %>


<%else%>
<ul class = "d-flex align-items-center">
<%= link_to 'Log in', login_path, class: "text-white mr-5" %>
<%= link_to 'Sign up', signup_path, class: "text-white mr-5" %>
</ul>
  <%end%>
  </div>
</nav>


