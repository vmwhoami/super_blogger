<h1 align="center">This shows the info for <span class="text-danger"> <%= @user.username%></span> </h1>

<div class = "row">
  <div class ="col-md-4 col-md-offset-4 center">
  <%= gravatar_for @user %>
  </div>
</div>
<h4>This is the user's  <%=@user.username%> articles</h4>
<%= render 'articles/article',obj:@user.articles %>

<%= link_to 'Edit user', edit_user_path %>